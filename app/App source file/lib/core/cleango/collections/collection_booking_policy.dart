class CollectionBookingPolicy {
  const CollectionBookingPolicy._();

  static const maxCustomerNotesLength = 500;
  static const maxQuoteImageCount = 4;
  static const maxQuoteImageBytes = 4 * 1024 * 1024;
}
